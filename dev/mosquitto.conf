# Seed configuration for the `mosquitto` service in docker-compose.dev.yml.
# Anonymous connections only, on the compose network — this broker is not
# reachable outside it and carries no real credentials.
listener 1883
allow_anonymous true
persistence false
